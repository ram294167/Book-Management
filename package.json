{
  "name": "book-management",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "start:api": "json-server --watch db.json --port 4000",
    "start": "npm-run-all --parallel start:api dev"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "json-server": "^0.17.3",
    "npm-run-all": "^4.1.5"
  }
}
